@AGENTS.md

<!--
The playbook lives in AGENTS.md so every coding agent reads the same file.
Claude Code reads CLAUDE.md rather than AGENTS.md, so the import above pulls
it in. Edit AGENTS.md, not this file. Claude-Code-specific instructions (if
any are ever needed) go below the import.
-->
